# AUTO-GENERATED from release.hone — do not edit directly.
# Source: .github/workflows/release.hone
#
name: Release
"on":
  push:
    tags: [v*]
permissions:
  contents: write
env:
  CARGO_TERM_COLOR: always
jobs:
  build:
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            archive: tar.gz
          - target: x86_64-apple-darwin
            os: macos-latest
            archive: tar.gz
          - target: aarch64-apple-darwin
            os: macos-latest
            archive: tar.gz
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            archive: zip
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - name: Build
        run: cargo build --release --target ${{ matrix.target }}
      - name: Package (Unix)
        if: "matrix.archive == 'tar.gz'"
        run: |-
          cd target/${{ matrix.target }}/release
          tar czf ../../../hone-${{ matrix.target }}.tar.gz hone
          cd ../../..
      - name: Package (Windows)
        if: "matrix.archive == 'zip'"
        shell: bash
        run: |-
          cd target/${{ matrix.target }}/release
          7z a ../../../hone-${{ matrix.target }}.zip hone.exe
          cd ../../..
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: hone-${{ matrix.target }}
          path: hone-${{ matrix.target }}.${{ matrix.archive }}
  release:
    needs: [build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: artifacts/**/*
